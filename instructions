requirements
1.php>8.1
2.composer
3.mysql
4.laragon/xampp
5.node.js+npm

step to replicate
1.Clone the repository
2.Copy .env file

  DB_CONNECTION=mysql
  DB_HOST=127.0.0.1
  DB_PORT=3306
  DB_DATABASE=devices
  DB_USERNAME=root
  DB_PASSWORD=


  3.install dependencies
    -composer install

  4.create mini mysql database
    make sure mysql connection info is align with .env , and create database "devices"

  5.run migrations and seeders
    -php artisan migrate --seed

  6.run the laravel server
    -php artisan serve

 how to use
 1. add a devices 
    - go to devices , + add devices
 2. activate a device
    - click activate -> transactions will start generating
 3. view transactions 
    - go to transactions -> go to Transactions page to see all generated data
 4. for transaction demo,
    - run php artisan queue:work
